### 问题 1：表格乱码问题：如果 PDF 里包含复杂表格，page.get_text() 提取出来的文字行列是错乱的。你未来打算怎么处理？
- page.get_text() 默认按阅读顺序输出，表格的行列关系会丢失。如果合同涉及复杂表格，我会改用 page.get_text("dict") 或 page.get_text("blocks")，它们返回带坐标的文本块。然后根据 bbox（边界框）的 y0 坐标分组，同一行的文本拼接，从而实现表格的结构化还原。
- 如果业务需要高精度表格提取，也可以引入 pymupdf 的 page.get_table() 方法（PyMuPDF 1.23+ 开始支持）。

### 问题 2：如果 PDF 有 500 页，一次性把全文读进内存做 join，内存可能爆掉。未来打算怎么优化？
- 流式入库：解析器不返回完整 full_text，而是在遍历每一页时，直接调用后续的切片（Chunking）逻辑，把每页产生的切片存进向量库。这样内存里永远只有当前这一页 + 已生成的切片。
- 分块存储：如果必须保留全文（比如为了元数据抽取），把解析后的文本按页写入临时文件（如 parsed_pages/），只把文件路径返回给主流程。后续需要全文时再从磁盘流式读取。

面试话术：
“在我的学习版实现中，我用了全量加载便于调试。但在生产设计上，我会把解析和切片做成 Pipeline（流水线），解析一页、切一页、存一页，避免单点内存峰值。”

### 问题 3：异常是抛上去还是返回空？
决策逻辑（没有标准答案，看场景）：

方案 A（你现在的做法，抛异常）：适用于“PDF 损坏属于业务异常，需要通知用户重新上传”的场景。优点是错误不会被忽视，用户体验清晰。

方案 B（返回空字符串）：适用于“这个 PDF 只是辅助资料，缺失不影响核心功能”的场景。优点是系统健壮，不会因为一个坏文件拖垮整个服务。

面试回答套路：
“我选择抛异常，因为合同审查中，PDF 解析失败意味着后续的检索和审查完全没有依据，属于不可恢复错误，必须让用户感知到。但如果是闲聊类机器人，我可能会选择返回空，让系统继续运行。”

### 长度陷阱：如果“第十条”内容足足有 3000 字（超过了 LLM 上下文窗口或 Embedding 模型最大长度），你的算法会把整个长条款切进去。

| 层次	| 方案	| 适用场景 |
|- | - | - |
| 初级方案（固定尺寸）	| 在“条款级切割”的基础上，如果单条超过阈值（比如 800 字），按句子再次切分（句号、分号），但保留 parent_chunk_id 指向原条款。检索时先查子块，再返回父块给 LLM。 | 	长条款不多的场景 |
|中级方案（滑动窗口）	|用 RecursiveCharacterTextSplitter（LangChain 内置）按 ["\n\n", "\n", "。", "，", " "] 优先级递归切，设置 chunk_overlap=200 保留上下文重叠。	|兼顾长度和语义连贯性|
|高级方案（语义切片）	|用 SentenceTransformer 计算相邻句子的 Embedding 余弦相似度，相似度突降处作为切分点。	|追求最优语义边界|

你的项目话术：
“在我的学习版中，我采用了条款级切割（业务语义明确）。如果遇到超长条款，我会引入 LangChain 的 RecursiveCharacterTextSplitter 做二次降级，并保留 parent_id 实现父子块关联。”

### 标题缺失：如果收到的 PDF 是纯英文合同，没有“第X条”，你的 re.finditer 返回空列表，整个文本就丢了。你现在的代码会怎么处理？请在代码逻辑里体现降级策略（如果没找到条款，直接把全文当成唯一的一个大块）。
你已经实现了 if not matches: return [{"chunk_id": 0, "title": "全文", "content": full_text}]，这已经是最稳妥的降级——保证系统永远不崩。

进阶话术（面试加分） ：
在检测到没有中文条款时，我会进一步尝试匹配英文合同的 Article 1、Section 1.1 等模式，多级回退（Fallback）。如果都匹配不到，说明这份合同是纯叙述性文本，这时我会回退到 固定长度切片（如 512 token），确保后续的向量检索依然可用。系统设计上，我不会因为格式异常而拒绝服务。

### ID 的不可变性：你现在用 chunk_id 从 1 递增。如果将来合同新增了“第一条 A 附录”，所有后续的 ID 都会变，向量库里的旧向量就全废了。思考一下，生产环境应该如何设计这个 chunk_id？（提示：哈希、UUID、或者 page_num 组合）
你说到了生产环境最痛的痛点：向量锁（Vector Locking）。

标准解：采用 “内容寻址” 或 “业务语义 ID”，而不是自增数字。

|方案	|实现方式	|优点|
|-|-|-|
|方案 A：哈希 ID	|chunk_id = hashlib.md5( (title + content).encode() ).hexdigest()[:16]	|内容不变，ID 永远不变，天然支持内容去重
|方案 B：路径式 ID	|chunk_id = f"{contract_md5}_{page_num}_{chunk_index}"	|可追溯来源，方便 Debug
|方案 C：版本化 ID	|chunk_id = f"v1_{title_hash}"，修订后生成 v2_xxx，旧向量依然可查	 |支持审计追溯

你的项目话术：
“目前学习版我用了自增 ID 便于调试。但在生产设计上，我会采用 MD5(标题 + 内容前100字符) 作为稳定 ID，确保同一条款在不同版本合同中被唯一标识，避免全量重建。